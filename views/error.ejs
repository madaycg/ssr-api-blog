<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/meta") %>
    <link href="/css/styles.css" rel="stylesheet" />
    <title>Error</title>
  </head>
  <body>
    <div class="container mt-90 mb-5 justify-content-center">
      <div class="col">
        <div class="row">
          <div class="card mt-5 shadow mb-3 text-center">
            <div class="card-body">
              <h5 class="card-title">Ocurrió un error al cargar los datos</h5>
              <!-- ejs no puede utilizar una variable que no esta definida 
                (algunos controladores muestran la vista sin enviar el error)-->
              <!-- por lo que nos aseguramos que este definida antes de utilizarla -->
              <p><%= (typeof error == 'undefined')?'':error %></p>

              <!-- The History.back() method causes the browser 
                  to move back one page in the session history -->
              <a href="#!" onclick="history.back()" class="card-link">Volver Atrás</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
