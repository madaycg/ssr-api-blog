<!DOCTYPE html>
<html lang="en">
  <head>
    <link href="/css/styles.css" rel="stylesheet" />
    <%- include("partials/meta") %>

    <title>Document</title>
  </head>
  <body class="black">
    <nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
      <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="/home">
          <img src="/img/ha_logo_600x197.png" alt="" />
        </a>
      </div>
    </nav>
    <div class="container mt-90 mb-5">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <div class="card mt-5 shadow mb-3">
            <div class="card-body">
              <h5 class="card-title">
                <% if(isCreate){ %> Crear <% } else{ %> Editar <% } %> Artículo
              </h5>
            </div>
            <div class="card-body">
              <form
                method="POST"
                action="
                <% if(isCreate){ %>
                  /admin/create
                <% } else{ %>  
                  /admin/<%=article.id%>
               <% } %> 
              "
              >
                <div class="mb-3">
                  <label for="titulo" class="form-label">Título</label>
                  <input
                    type="text"
                    class="form-control"
                    id="titulo"
                    name="titulo"
                    required
                    value="<%=article.title%>"
                  />
                </div>
                <div class="mb-3">
                  <label for="contenido" class="form-label">Contenido</label>
                  <textarea
                    class="form-control"
                    id="contenido"
                    name="contenido"
                    required
                    rows="10"
                  ><%=article.content%></textarea>
                </div>
                 <% if (isCreate) { %>
                  <div class="mb-3">
                  <label for="edad" class="form-label">Autor</label>
                  <input
                    type="text"
                    class="form-control"
                    id="autor"
                    name="autor"
                    required
                  />
                </div>
                <% } %>
                  <button type="submit" class="btn btn-primary">Guardar</button>
              </form>
            </div>
          </div>
          <a href="/admin">Atrás</a>
        </div>
      </div>
    </div>
  </body>
</html>
